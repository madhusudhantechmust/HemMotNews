/*------------------------------------------------------------------------------	sn	Source Name:	sn	HM_Teaser_Ad_Classes.hpp		au	Author:	au	Ross L. Edgar		da	Date	da	7/21/03 ca. 1:42 AM		ab	Abstract:	ab	Declarations for HM_Document and HM_Page.		cl	Change Log:	cl	4/10/11	Port to InDesign CS4.	cl	7/21/03	New.------------------------------------------------------------------------------*/#ifndef	HM_TEASER_AD_CLASSES_HPP#define	HM_TEASER_AD_CLASSES_HPP#include <string>#include <cstring>// The key classclass cl_ad_number {		std::string stlStr;	public:		cl_ad_number ()							{ stlStr = ""; }		cl_ad_number (std::string s)					{ stlStr = s; }		cl_ad_number (char* s)					{ stlStr = s; }		std::string Get_()							{ return stlStr; }};// Must define less than relative to cl_ad_number objects.inline bool operator< (cl_ad_number a, cl_ad_number b){   return a.Get_() < b.Get_();}// The value classclass cl_display_page_number {		std::string stlStr;	public:		cl_display_page_number ()					{ stlStr = ""; }		cl_display_page_number (std::string s)		{ stlStr = s; }		cl_display_page_number (char* s)			{ stlStr = s; }		std::string Get_()							{ return stlStr; }};#endif// END OF FILE